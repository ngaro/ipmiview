version: '3'
services:
    ipmiview:
        container_name: ipmiview
        image: garo/ipmiview:latest
        ports:
            - 8080:8080
        build:
          context: .
          dockerfile: Dockerfile
          args:
            IPMIVIEW_VERSION:
